###############################################################################
# Distribute Vault client TLS material (Vault → Consul)
###############################################################################

- name: Ensure Vault TLS directory exists
  file:
    path: "{{ vault_tls_dir }}/consul-client"
    state: directory
    owner: vault
    group: vault
    mode: "0750"


